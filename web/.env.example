# Postgres connection used by the Next.js server components
# (the same database the indexer writes to).
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ligate_explorer

# Public chain id, surfaced in the UI when the indexer hasn't
# populated indexer_state yet. Optional.
NEXT_PUBLIC_CHAIN_ID=ligate-devnet-1
